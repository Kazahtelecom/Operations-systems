#!/bin/bash
set -euo pipefail

LOG_DIR="${1:-./logs}"

if [[ ! -d "$LOG_DIR" ]]; then
    echo "Ошибка: директория '$LOG_DIR' не найдена"
    exit 1
fi

echo "=== Статистика логов в $LOG_DIR ==="
total=0

for file in "$LOG_DIR"/*.log; do
    if [[ -f "$file" ]]; then

